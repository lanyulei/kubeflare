{
  "name": "adv-out-of-scope-cni",
  "question": "两个 Pod 之间网络不通,curl 对方 IP 超时",
  "scope": {"namespace": "default", "resource_kind": "Pod"},
  "tool_responses": {
    "cluster.pod.get": {
      "observation": "Pod a-1 与 b-1 均 Running、就绪正常,各自有 PodIP,无异常容器状态"
    },
    "cluster.service.get": {
      "observation": "相关 Service 正常,Endpoints 就绪"
    },
    "cluster.event.list": {
      "observation": "无网络相关 Warning 事件"
    }
  },
  "steps": [
    {"content": "看两端 Pod 状态", "tool_calls": [{"tool": "cluster.pod.get", "arguments": "{\"namespace\":\"default\",\"resource_name\":\"a-1\"}"}]},
    {"content": "看 Service", "tool_calls": [{"tool": "cluster.service.get", "arguments": "{\"namespace\":\"default\"}"}]},
    {"content": "看事件", "tool_calls": [{"tool": "cluster.event.list", "arguments": "{\"namespace\":\"default\"}"}]}
  ],
  "conclusion": "### 结论\n两端 Pod 与 Service 在 Kubernetes 资源层均正常,故障很可能位于当前只读工具视野之外的网络层(CNI/NetworkPolicy/节点路由),无法仅凭现有证据确定根因。\n### 证据\n- [E1] pod.get: 两端 Pod Running 且有 PodIP\n- [E2] service.get: Endpoints 正常\n- [E3] event.list: 无网络告警\n### 建议\n- 需在节点/CNI 层(NetworkPolicy、kube-proxy、CNI 插件日志)继续排查,这超出当前只读工具范围\n### 准确性提示\n资源层无异常不代表网络通,根因可能在本工具集无法观测的网络层。",
  "expected": {
    "root_cause_keywords": [],
    "expected_tools": ["cluster.pod.get", "cluster.service.get"],
    "must_not": ["OOM", "镜像拉取", "调度失败"],
    "must_include": ["无法", "网络"]
  }
}
